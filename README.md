# mutlibrary

This is a package to generate mutation libraries from input sequences.

## Features

- `src/`-layout
- Poetry for dependency & build management
- Black + Ruff + Mypy + pytest
- Sphinx + MyST + Furo docs
- Pre-commit hooks
- GitHub Actions CI

## Getting started

```bash
poetry install
poetry run pre-commit install
poetry run pytest
